# Base Contract Deployer CLI

Minimal CLI script to deploy any compiled contract (ABI + bytecode) to Base mainnet using ethers.js.

⚠️ Never commit your PRIVATE_KEY to GitHub — use environment variables only.
